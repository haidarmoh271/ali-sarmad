{
  "name": "project-eco",
  "private": true,
  "scripts": {
    "build": "cd ecommerce && npm install && npm run build",
    "start": "cd ecommerce && npm run preview -- --host 0.0.0.0 --port ${PORT:-4173}"
  }
}
